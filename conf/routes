# MarketTrade Dashboard
GET         /                    controllers.Application.index
GET         /tradedash           controllers.Application.index
POST        /trademsg            controllers.Application.trade
GET         /tradestream         controllers.Application.tradeStream

# Map static resources from the /public folder to the /assets URL path
GET         /assets/*file        controllers.Assets.at(path="/public", file)
